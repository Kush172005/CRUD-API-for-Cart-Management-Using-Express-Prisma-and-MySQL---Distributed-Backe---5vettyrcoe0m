generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Cart {
  cartId    Int @id @default(autoincrement())
  userId    Int
  productId Int
  count     Int
}
